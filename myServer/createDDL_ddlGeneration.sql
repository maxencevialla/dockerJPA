CREATE TABLE CHARACTER (ID INTEGER NOT NULL, LEVEL INTEGER, NAME VARCHAR, PRIMARY KEY (ID))
CREATE TABLE ITEM (ID INTEGER NOT NULL, COST INTEGER, DESCRIPTION VARCHAR, NAME VARCHAR, PRIMARY KEY (ID))
CREATE TABLE CHARACTER_ITEM (Character_ID INTEGER NOT NULL, items_ID INTEGER NOT NULL, PRIMARY KEY (Character_ID, items_ID))
ALTER TABLE CHARACTER_ITEM ADD CONSTRAINT FK_CHARACTER_ITEM_items_ID FOREIGN KEY (items_ID) REFERENCES ITEM (ID)
ALTER TABLE CHARACTER_ITEM ADD CONSTRAINT FK_CHARACTER_ITEM_Character_ID FOREIGN KEY (Character_ID) REFERENCES CHARACTER (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
